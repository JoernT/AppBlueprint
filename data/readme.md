This directory is intended to hold xml data for the application.

Note:

if your application becomes big and holds a lot of data it might be advisable to separate those out into a separate application archive.